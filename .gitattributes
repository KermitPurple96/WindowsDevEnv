# Normalize line endings so the CRLF warnings stop and diffs stay clean.
* text=auto eol=lf

# PowerShell is conventionally CRLF on Windows
*.ps1 text eol=crlf

# These are always LF
*.lua  text eol=lf
*.json text eol=lf
*.md   text eol=lf
*.toml text eol=lf
